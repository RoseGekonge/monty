#include <stdio.h>
#include <stdlib.h>
#include "monty.h"

/**
 * main - executes monty
 * @argc: number of arguments
 * @argv:arguments passed
 * Return: 0(success) or 1(failure)
 */

int main(int argc, char *argv[])
{
	int stack[];
	int input;

	if (strcmp(input, "push") == 1)
	{
